import { createIcon } from './createIcon';
import { home } from '../icons/generated/home';
import { search } from '../icons/generated/search';
import { user } from '../icons/generated/user';
import { x } from '../icons/generated/x';
import { menu } from '../icons/generated/menu';
import { heart } from '../icons/generated/heart';
import { star } from '../icons/generated/star';
import { check } from '../icons/generated/check';

export const HomeIcon = createIcon(home);
export const SearchIcon = createIcon(search);
export const UserIcon = createIcon(user);
export const XIcon = createIcon(x);
export const MenuIcon = createIcon(menu);
export const HeartIcon = createIcon(heart);
export const StarIcon = createIcon(star);
export const CheckIcon = createIcon(check);
